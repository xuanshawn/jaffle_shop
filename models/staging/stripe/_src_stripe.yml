sources:
  - name: stripe
    database: dbt-tutorial
    schema: stripe
    tables:
      - name: payment
        columns:
          - name: status
            description: '{{doc("payment_status")}}' #if single quotes inside, use double quotes outside
            data_tests:
              - accepted_values:
                  arguments:
                    values: ["succeeded", "failed", "pending"]



