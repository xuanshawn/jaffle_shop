version: 2

macros:
  - name: cents_to_dollars
    description: "Converts a column from cents to dollars"
    arguments:
      - name: column_name
        description: "The column to convert"
        type: string